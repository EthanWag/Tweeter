
// 1. Import necessary modules
export const handler = async () => {

    // 2. Make a feed service object and call the associated method

    // 6. put inside the queue the bunches of UpdateFeedRequests

}